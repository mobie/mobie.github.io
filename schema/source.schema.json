{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/mobie/mobie.github.io/master/schema/source.schema.json",
    "title": "MoBIESourceSchema",
    "description": "Schema describing a MoBIE source",

    "definitions": {
        "sourceLocations": {
            "description": "Location of the bdv.xml files for this source, relative to the dataset root directory.",
            "type": "object",
            "properties": {
                "local": {
                    "description": "",
                    "type": "string"
                },
                "remote": {
                    "description": "",
                    "type": "string"
                }
            },
            "required": ["local"],
            "additionalProperties": false
        },

        "imageSource": {
            "type": "object",
            "properties": {
                "sourceLocations": {"$ref": "#/definitions/sourceLocations"},
                "menuItem": {
                    "description": "'",
                    "type": "string"
                },
                "view": {
                    "description": "",
                    "$ref": "https://github.com/mobie/mobie.github.io/schema/view.schema.json"
                }
            },
            "required": ["sourceLocations", "menuItem", "view"],
            "additionalProperties": false
        },

        "segmentationSource": {
            "type": "object",
            "properties": {
                "sourceLocations": {"$ref": "#/definitions/sourceLocations"},
                "menuItem": {
                    "description": "",
                    "type": "string"
                },
                "tableRootLocation": {
                    "description": "Location of the table root directory for this segmentation source, relativeto the dataset root directory.",
                    "type": "string"
                },
                "view": {
                    "description": "",
                    "$ref": "https://github.com/mobie/mobie.github.io/schema/view.schema.json"
                }
            },
            "required": ["sourceLocations", "menuItem", "view"],
            "additionalProperties": false
        }
    },

    "type": "object",
    "oneOf": [
        {
            "properties": {
                "image": {"$ref": "#/definitions/imageSource"}
            },
            "required": ["image"],
            "additionalProperties": false
        },
        {
            "properties": {
                "segmentation": {"$ref": "#/definitions/segmentationSource"}
            },
            "required": ["segmentation"],
            "additionalProperties": false
        }
    ]
}
